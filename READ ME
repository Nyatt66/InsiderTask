Installation
Clone this repository to your local machine using the following command:

Use this command:
" git clone https://github.com/Nyatt66/InsiderTask.git "


Build the project and resolve dependencies using Maven:

Use this command:
" mvn clean install "

---------------------------------------------------

Usage
After successfully installing the project, navigate to the root directory.

Modify the test scripts or create new ones according to your requirements.

Run the tests using the following command:

Use this command:
" mvn site "

The tests will be executed, and the results will be displayed in the console.

---------------------------------------------------

Reports
the executed test results will be in -> target\site\surefire.html
